import CookiePolicyContent from "./cookies-content";

export const metadata = {
  title: "Cookie Policy — MBN Health",
};

export default function CookiePolicyPage() {
  return <CookiePolicyContent />;
}
